# ---- base system ----
base
base-devel
linux
linux-headers
# A second kernel to fall back to. A driver regression in the newest kernel is the
# commonest cause of "it worked last month", and without this the only recovery a
# student has is a reinstall.
linux-lts
linux-lts-headers
linux-firmware
amd-ucode
intel-ucode
mkinitcpio
mkinitcpio-archiso
sof-firmware
sudo
zsh
bash-completion
man-db
man-pages
git
wget
curl
rsync
less
which
usbutils
pciutils
htop
btop
fastfetch
# CPU temperature for the System Health bar widget (cyberos-systemhealth-state).
# Without it the widget still works -- temperature just reads "n/a" -- but a
# health panel with no temperature reading is a degraded one, not an optional
# extra, on hardware this project already assumes runs hot under lab load.
lm_sensors
unzip
zip
7zip
tree
jq

# ---- installer / disk tools ----
arch-install-scripts
archinstall
dosfstools
e2fsprogs
btrfs-progs
exfatprogs
ntfs-3g
gptfdisk
parted
efibootmgr
grub
syslinux
os-prober
testdisk
squashfs-tools
dialog
memtest86+
memtest86+-efi
edk2-shell

# ---- network ----
networkmanager
modemmanager
wireless-regdb
wpa_supplicant
openssh
bind
nmap
tcpdump
wireshark-qt
openvpn
wireguard-tools
inetutils
traceroute
net-tools
whois

# ---- firewall ----
ufw

# ---- graphics / wayland stack ----
mesa
vulkan-intel
vulkan-radeon
intel-media-driver
xorg-xwayland
qt5-wayland
qt6-wayland

# Drivers for hardware students actually own, not just what QEMU presents.
# nvidia-open supports Turing (GTX 16xx / RTX 20xx) and newer only; older cards
# fall back to nouveau and are Tier 3 in docs/SPEC.md.
nvidia-open-dkms
# Broadcom wireless is common on the older Dell and Apple laptops students bring
# and is not covered by linux-firmware.
broadcom-wl-dkms
# Several Tier 2 laptop failures are firmware bugs with an LVFS fix.
fwupd
# Power profiles (power-saver / balanced / performance) for the battery chip's
# panel. Quickshell talks to it over D-Bus; without the daemon the panel renders
# but every profile switch silently does nothing.
power-profiles-daemon
# CyberOS has no Microsoft-signed shim and cannot claim Secure Boot support, but
# sbctl lets a lab enrol its own keys rather than leaving those machines
# unbootable. docs/SPEC.md S5.
sbctl

# ---- Hyprland desktop ----
hyprland
swaybg
hyprlock
hypridle
hyprpolkitagent
xdg-desktop-portal-hyprland
# Qt file-chooser/settings portal -- hyprland's portal has no FileChooser.
# Replaces xdg-desktop-portal-gtk; /etc/xdg-desktop-portal/hyprland-portals.conf
# routes FileChooser here.
xdg-desktop-portal-kde
# removable-drive mounting for the QML file manager
udisks2
xdg-user-dirs
quickshell
grim
slurp
wl-clipboard
cliphist
# qalc (libqalculate's CLI) backs the QML calculator popup (Super+equal). It
# used to arrive transitively as rofi-calc's dependency; explicit now that
# rofi-calc is gone.
libqalculate
brightnessctl
playerctl
pamixer
kanshi
# notify-send (Super+Shift+U touchscreen toggle) posts through the same
# D-Bus notification pipeline as everything else, landing in the shell's
# own NotifyPopups bubble instead of a visually inconsistent compositor
# bezel.
libnotify

# ---- display manager ----
sddm
qt6-declarative
qt6-svg
qt6-5compat
qt5-graphicaleffects
qt5-quickcontrols2
qt5-svg

# ---- audio / bluetooth ----
pipewire
pipewire-alsa
pipewire-pulse
pipewire-jack
wireplumber
bluez
bluez-utils

# ---- terminal / editors ----
foot
foot-terminfo
tmux
neovim
python-pynvim
ripgrep
fd
fzf
lazygit

# ---- apps ----
firefox
# Firefox is also the PDF viewer (pdf.js) -- okular is gone with the rest of
# the KDE apps. Files/Images/Mixer are Quickshell QML surfaces now, so the
# only GUI apps on the ISO are the two that no QML surface can replace.
mpv
# xdg-open: how the QML file manager hands a file to its registered handler.
# Shell scripts, no toolkit dependency. Nothing else on the ISO provides it
# (dolphin used KIO internally).
xdg-utils
# trash-put: the freedesktop trash spec. `gio trash` left with gvfs, and the
# file manager must never rm a student's work.
trash-cli

# ---- cloud drives ----
# Connect iCloud/Google Drive/OneDrive as folders under ~/Cloud (bar chip +
# popups/CloudDrives.qml, backed by cyberos-cloud-drives). All base packages
# rather than a runtime install -- consistent with lm_sensors above, and
# this project's own machines are not guaranteed a network connection at
# first login the way Omarchy's own omarchy-pkg-add lazy-install assumes.
rclone
fuse3
gum
libsecret
# The config-encryption password and (for iCloud) the Apple ID password
# live only here, never on disk in the clear -- see cyberos-cloud-drives'
# own header comment. Started at session autostart (hyprland.lua), not
# PAM-unlocked at login: this project's own lab-machine threat model
# (shared machines, no single-owner laptop-theft scenario) doesn't carry
# PAM's added complexity/blast-radius for a marginal benefit against a
# threat that mostly doesn't apply here.
gnome-keyring

# ---- virtualization ----
virtualbox
virtualbox-host-dkms
virtualbox-guest-utils
qemu-guest-agent
open-vm-tools

# ---- fonts / theme ----
ttf-jetbrains-mono-nerd
ttf-firacode-nerd
noto-fonts
noto-fonts-cjk
noto-fonts-emoji
ttf-liberation
ttf-dejavu
papirus-icon-theme
adwaita-cursors
qt6ct

# ---- dev basics (for VS Code / Neovim workflows) ----
python
python-pip
gcc
gdb
make
cmake
nodejs
npm
sqlite
strace
lsof
socat
openbsd-netcat

# ---- security lab toolset ----
john
hashcat
hydra
aircrack-ng
sqlmap
nikto
gobuster
binwalk
radare2
ghidra
metasploit
exploitdb
hashcat-utils
masscan
arp-scan
bettercap
impacket
python-scapy
smbclient
nbtscan
sslscan
testssl.sh
foremost
perl-image-exiftool
sleuthkit
volatility3
yara
clamav
lynis
docker
docker-compose
# CVE reporting against the pinned channel. docs/SPEC.md S4.
arch-audit

# ---- from the local [cyberos] repo (AUR + local PKGBUILDs, built by build.sh) ----
pixie-sddm-git
visual-studio-code-bin
onlyoffice-bin
#packettracer   # re-enable once CiscoPacketTracer_*.deb is in aur/
